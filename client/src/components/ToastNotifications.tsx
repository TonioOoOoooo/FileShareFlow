import { useToast } from "@/hooks/use-toast";

export default function ToastNotifications() {
  // This component is just a placeholder since we're using the shadcn/ui Toaster component
  // The actual toast notifications are managed through the useToast hook
  return null;
}
